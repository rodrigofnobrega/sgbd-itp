#ifndef UTILS_H
#define UTILS_H

#include <stdio.h>
#include <string.h>
#include <stdlib.h>

#define DB_PATH "../database/"

int remover_linha(const char *nome_tabela, const char *chave_primaria);

#endif 
